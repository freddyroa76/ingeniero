// Navigation logic for calculator tools
document.addEventListener("DOMContentLoaded", () => {
  // The navigation is now hardcoded in the HTML files for better performance and SEO.
  // This script can be used for shared interactive behaviors in the future.
});
